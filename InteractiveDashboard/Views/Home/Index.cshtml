<style>
    html, body {
        height: 100%;
        margin: 0;
        overflow: hidden;
    }

    body {
        background-image: url("image/Screenshot 2026-09-10 210456.png");
        background-repeat: no-repeat;
        background-position: center center;
        background-size: cover;
        min-height: 100vh;
    }

    .home-content {
        height: calc(100vh - 70px);
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        overflow: hidden;
    }

        .home-content h1.title {
            font-family: 'Montserrat', Arial, sans-serif;
            font-weight: 700;
            letter-spacing: 3px;
            color: #fff;
            font-size: 2.5rem;
            margin: 0;
        }

        .home-content h1.subtitle {
            font-family: 'Allura', cursive;
            font-weight: 400;
            color: #d4af37;
            font-size: 3.5rem;
            margin: 0 0 20px 0;
        }

        .home-content p {
            font-family: 'Poppins', Arial, sans-serif;
            font-weight: 300;
            color: #fff;
            font-size: 1.3rem;
            margin-top: 20px;
        }
</style>


